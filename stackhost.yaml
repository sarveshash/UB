user:
  name: Otazuku
  github: Otazuki004

language: "python"
python_version: "3.12"

commands:
  build:
    - "pip install -U -r requirements.txt"
  package:
    - "ffmpeg"
    - "build-essential"
    - "libffi-dev"
  start:
    - "python3 -m Sophia"

env:
  - "ENVIRONMENT=production"
